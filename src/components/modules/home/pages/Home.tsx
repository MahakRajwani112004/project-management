import CustomProjectHeader from '../../../CustomProjectHeader';
import TaskBoard from '../components/TaskBoard';

const Home = () => {
	return (
		<div>
			<CustomProjectHeader title="Mobile App" />
			<TaskBoard />
		</div>
	);
};

export default Home;
